# design-sync notes for bb

## Setup and build order

- The design system is `packages/shared-ui` (`@bb/shared-ui`), a source-only package with per-component subpath exports, no `dist/`, no barrel. `apps/app/src/components/ui/` holds Ladle stories and app wrappers, not the primitives.
- There's no Storybook; bb uses Ladle, so this runs in the `package` shape.
- Run the converter with `--node-modules apps/app/node_modules` and NO `--entry`. That dir has `@bb/shared-ui` symlinked in, so PKG_DIR resolves to the package and the converter builds its entry from `src/`. Passing `--entry packages/shared-ui/package.json` gets treated as a JS entry and finds zero components.
- Because PKG_DIR is the symlink path (`apps/app/node_modules/@bb/shared-ui`), every package-relative config path (`extraFonts`, `docsDir`) resolves lexically from there, not from `packages/shared-ui`. That's why `docsDir` is `../../../../../.design-sync/docs`.
- Type contracts: write `packages/shared-ui/index.d.ts` (gitignored) as `export * from "./src/components/ui/<file>"` for every non-test `.tsx` in `src/components/ui/`. Without it every `<Name>.d.ts` comes out `[key: string]: unknown`. The converter reads `<pkg>/index.d.ts` by default and ts-morph resolves through to the `.tsx` source.
- CSS is Tailwind v4 compiled from `apps/app/src/components/ui/theme.css` (which `@source`s `apps/app/src` and `shared-ui/src`). `cfg.buildCmd` compiles `.design-sync/tailwind.css` (imports theme.css, adds `@source` for `.design-sync/previews`) into `packages/shared-ui/.ds-css/bb.css`. `cssEntry` has to live under PKG_DIR, hence the package-local output. **Re-run buildCmd after editing any preview**, or new utility classes in the preview won't exist.
- `@tailwindcss/cli@4.3.3` and `tw-animate-css` get installed into `.ds-sync/` alongside the converter deps. The repo lockfile isn't touched.
- Fonts: `@fontsource-variable/inter` via `extraFonts` (`../../@fontsource-variable/inter/index.css`, relative to the symlinked PKG_DIR).

## Render check environment

- Playwright's Chromium download failed twice (sandbox blocked storage.googleapis.com, then the CDN connection dropped mid-zip). Use system Chrome instead: `DS_CHROMIUM_PATH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"`.
- Chrome can't launch inside the Claude Code command sandbox (`browserType.launch: Target page, context or browser has been closed`). Run `package-validate.mjs` / `package-capture.mjs` with the sandbox disabled.

## Scope decisions (first sync, 2026-09-15)

- Pilot: authored previews for Button, Pill, Badge, Card, Input, Switch, Alert, Tooltip. Everything else ships on the floor card.
- Re-sync (2026-09-15, same day): authored previews for the rest of Actions and Feedback — Toggle, ToggleGroup, ToggleGroupItem, AlertTitle, AlertDescription, DelayedLoading, Skeleton, WorkflowStatusPill, WorkflowPhaseStrip, WorkflowProgress. 18/271 now authored; everything else still ships on the floor card. `AlertTitle`/`AlertDescription` previews compose inside a real `Alert` (they're context-free HTML elements, but that's the only render that matches actual usage). `ToggleGroupItem`'s preview composes inside a `ToggleGroup` — Radix's item needs the group's roving-focus context, it isn't renderable standalone. `DelayedLoading`'s 200ms mount-delay resolved before capture's screenshot both times; if a future capture run ever shows it blank, that's a timing race, not a real regression — don't chase it, just re-run capture.
- Next candidate batches, by floor-card count: Overlays (106), Resource (54), Data display (30 minus what's now authored), Navigation (24), Layout (14). Josh wants a plugin-usage survey (which components existing plugins actually render) to prioritize which of these get authored next, rather than picking by section size alone — do that before committing to the next batch's scope.
- Plugin usage survey (2026-09-16, against `origin/main` at 62fe3bc46): tallied non-test `import { … } from "@bb/shared-ui/<mod>"` across `plugins/*` (24 of 27 import shared-ui) and `examples/plugins/*` (none do; they use only `@get-bb/plugin-sdk`). External plugins can't import `@bb/shared-ui`. Components were removed from the SDK surface on 2026-07-03, and external authors vendor source via `npx shadcn add @bb/<name>` from `packages/plugin-registry/registry.json`. So in-repo plugins are the only usage signal. Of the 253 floor-card components, only 79 are used by any plugin. By category: Overlays 40/106, Resource 20/54, Forms 10/25, Navigation 4/24, Layout 4/14, Data display 1/30. All 20 Resource components are used by `automations` alone. The highest-usage gaps are Dialog* (7 plugins), DropdownMenu* (6), Select* (5), and the held-back `Icon` (11 plugins), `Textarea` (4), and `Checkbox` (2). Then come Tabs*, Popover*, and Command*/ContextMenu* (tasks only). `BranchPickerRow`/`BranchPickerSearch`/`BranchPickerSectionHeader` and `QuestionForm` are used by plugins but missing from `.design-sync/docs/` (added to shared-ui after the pilot snapshot), so a re-sync on current main needs stubs for them. `theme-preview` is a showcase plugin and inflates the breadth counts slightly.
- Marketplace survey (2026-09-16): pulled `https://getbb.app/marketplace/v2/marketplace.json`, which has 199 plugins and no install/star counts, so no "top N" exists. Shallow-cloned all 149 source repos plus the 12 npm packages, then tallied named imports from vendored `components/ui/<name>` paths (via `components.json`, shadcn-style) per plugin `subdir`. Of 197 plugins resolved, 82 use at least one bb UI component. Breadth ranking matches the in-repo survey: Button 72, Icon 53 (held back!), Input 47, Dialog* ~29, Card/CardContent 16/14, Select* 16, DropdownMenu* 15, Tooltip subparts 15, Textarea 12 (held), Card subparts 10, Tabs* 8, Checkbox 8 (held), AlertDialog* 7, ContextMenu* 6. Zero marketplace plugins use any `Resource*` component; that category is `automations`-only and should be authored last. Install counts do exist, just not in the manifest: the SSR payload of `https://getbb.app/marketplace` embeds `stats: { generatedAt, plugins: { <id>: { installs } } }` for 212 ids, first-party plugins included (`tasks` 1020, `github` 817; 12,014 total). Weighting each component by the installs of the plugins that use it, the ranking of what's not authored yet: Icon (29% of installs, held), Textarea (26%, held), Select* (25%), DropdownMenu* (22%), Dialog* (21%), Tooltip subparts (19%), Tabs* (19%), Popover*/Command*/ContextMenu* (~10%). Card subparts fall to ~1% and Resource to ~0%. 9 of the top 20 plugins by installs use no bb UI components at all. The scratch scripts (`tally.mjs`, `tally-ext.mjs`) lived in the session scratchpad and weren't kept.
- Held back via `componentSrcMap: null` (render near-blank without props, need authored previews first): Avatar, BreadcrumbEllipsis, InputOTPSeparator, PaginationEllipsis, Progress, Slider, ResourceActionButton, ResourceIconFrame, ResourceMultiSelectMenu, ResourceOptionMenu, ResourceOverflowMenu, ResourceRowDetailChevron. They're still in `_ds_bundle.js` (`window.BbUI.Icon` works); they just have no card or `.d.ts` in the project. Checkbox/Icon/Textarea were cleared from this list in batch 3 (below) — they now have authored previews and real `.d.ts` contracts.
- Batch 3 (2026-09-16): authored previews for the 3 held-back components with the highest marketplace reach (Checkbox, Icon, Textarea — cleared their `componentSrcMap: null` entries) plus the Select family (7) and DropdownMenu family (12) and Dialog family (8) — 30 components total, chosen from the plugin-usage survey above. Icon/Textarea/Checkbox needed new `.design-sync/docs/<Name>.md` stubs (none existed before): Checkbox and Textarea got `category: Forms` (matches Switch/Label precedent for form-adjacent primitives); Icon got `category: Data display` after asking Josh — he picked Data display over inventing a new "Foundational" category for primitives, since only Icon would populate it right now (Avatar/Progress/Slider are the same kind of categoryless primitive but are still held back, not part of this batch). All Select/DropdownMenu/Dialog previews use each Radix root's `open` prop to force content visible for the render-check screenshot, same pattern as `Tooltip.tsx`. Two components needed `cfg.overrides` for `[GRID_OVERFLOW]`: `Textarea` → `{"cardMode": "column"}`, `DropdownMenuCheckboxItem` → `{"cardMode": "single", "primaryStory": "Default"}`. 48/253 components now authored total (18 from before + 30 this batch).
- Sections come from `.design-sync/docs/<Name>.md` stubs (`category` frontmatter + "Composes with" siblings from the same source file + an example for the authored 8). A stub replaces the synthesized prompt body, which is why the stubs carry the sibling list and examples themselves. They were generated from a file-to-section table; a new component needs its own stub or it lands ungrouped.
- `TooltipProvider` is the `cfg.provider`.

## Known render warns

- `[TOKENS_MISSING]` for `--bb-app-chrome-row-height`, `--bb-shell-height`, `--secondary-panel-width-mobile`, `--sidebar-width-mobile`, `--story-doc-width`, `--sidebar-width-icon`: set at runtime by app shell code, not in theme.css.
- `[FONT_MISSING] "Inter"`: it's only the fallback after `"Inter Variable"`, which ships.
- `[RENDER_THIN]` on ~20 `Resource*` domain composites (they render their own name through the component chrome with no data). Legit until authored.
- `PersistentResponsiveDrawerShell` floor card renders under the drawer's gray backdrop. Readable, leave it.

## Re-sync risks

- `.design-sync/docs/` is a snapshot of which components exist and which file they come from. Components added, renamed, or moved between files in `shared-ui` go stale silently (ungrouped, or a wrong "Composes with" list). The authored examples in `Button.md`, `Card.md`, etc. duplicate the preview `.tsx` and can drift from the component API.
- The held-back list was decided by the render check at this commit. A component that grows required props could start rendering blank and need adding.
- `packages/shared-ui/index.d.ts` must be regenerated when component files are added or removed; a stale one silently drops `.d.ts` contracts for new components.
- The compiled CSS only has classes the app uses at build time. If the app drops a utility, designs that relied on it lose styling on the next sync.
- Built against Tailwind 4.3.3, Node 22.19.0, system Chrome (not Playwright's pinned Chromium).
